/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[][]}
 */
var fourSum = function (nums, target) {
    console.log(nums)
    console.log(target)
    nums.sort((a,b)=>a-b)
    for(let a=0;a<nums.length;a++){
        console.log(nums[a])

        left = a+1
        // right= left+1;

    }

};


let nums = [1,0,-1,0,-2,2]
let target = 0
fourSum(nums,target)