/**
 * @param {number} n
 * @return {number[]}
 */

const countNoOfOneBits = (x) => {
  let count = 0
  console.log(count)
  console.log(x)

  while (x > 0) {
    console.log(x)
  }
  return count
}
var countBits = function (n) {
  console.log(n)
  counts = []
  for (let i = 0; i < n + 1; i++) {
    console.log(i)
    // counts.push(countNoOfOneBits(i))
  }
}

let n = 2
console.log(n)
// countBits(n)
